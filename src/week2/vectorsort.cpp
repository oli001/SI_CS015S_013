#include <iostream>
#include <vector>

using namespace std;

vector<int> sort(vector<int> a, int begin, int end, int find)
{
    if(a.size() = 1)
    {
        return a;
    }
    
    else if(find > a.at())
    
}

int main()
{
    return 0;
}